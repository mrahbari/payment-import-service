<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx</pattern>
        </encoder>
    </appender>

    <!-- GlobalExceptionHandlerTest intentionally triggers generic(); log.error(…, ex) would print a
         full stack (noise in CI, looks like a failure). Suppress this logger in tests only. -->
    <logger name="com.example.shared.error.GlobalExceptionHandler" level="OFF"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
